task :install do
  case
  when RUBY_VERSION.downcase.include?("darwin")
    sh "defaults write com.sublimetext.2 ApplePressAndHoldEnabled -bool false"
  when RUBY_VERSION.downcase.include?("")
end

def